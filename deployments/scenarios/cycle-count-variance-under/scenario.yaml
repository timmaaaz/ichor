name: cycle-count-variance-under
description: |
  Cycle count session at STG-A01: system expected 40 units of SKU-0001,
  worker counts 30. Tests the negative-variance (undercount) branch where
  counted_quantity < system_quantity, yielding variance = -10.
