{
    "type": "object",
    "properties": {
        "default_value": {
            "type": "string",
            "description": "Default value (supports {{$me}}, {{$now}} variables)"
        },
        "default_value_create": { "type": "string" },
        "default_value_update": { "type": "string" },
        "copy_from_field": {
            "type": "string",
            "description": "Copy value from this sibling field when this field is absent from submitted data"
        }
    }
}
