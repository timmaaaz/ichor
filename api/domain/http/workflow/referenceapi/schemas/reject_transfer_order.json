{
    "type": "object",
    "required": ["transfer_order_id", "rejection_reason"],
    "properties": {
        "transfer_order_id": {
            "type": "string",
            "format": "uuid",
            "description": "ID of the transfer order to reject"
        },
        "rejection_reason": {
            "type": "string",
            "description": "Reason for rejection, required for audit trail"
        }
    }
}
