{
    "type": "object",
    "properties": {
        "product_id": {
            "type": "string",
            "format": "uuid",
            "description": "Product to check inventory for (required unless source_from_line_item is true)"
        },
        "source_from_line_item": {
            "type": "boolean",
            "description": "Whether to source product_id from the triggered entity's line items"
        },
        "threshold": {
            "type": "integer",
            "minimum": 0,
            "description": "Inventory threshold to check against"
        },
        "warehouse_id": {
            "type": "string",
            "format": "uuid",
            "description": "Optional warehouse to scope the check"
        },
        "location_id": {
            "type": "string",
            "format": "uuid",
            "description": "Optional location to scope the check"
        }
    }
}
