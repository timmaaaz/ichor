services:
  database:
    image: postgres:16.4
    container_name: database
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=postgres
    volumes:
      - ./database-data:/var/lib/postgresql/data
      - ./pg_hba.conf:/etc/pg_hba.conf
    command: [ '-c', 'hba_file=/etc/pg_hba.conf' ]
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -h localhost -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    cpu_count: 2
    networks:
      ichor-system-network:
        ipv4_address: 10.5.0.2

  init-migrate-seed:
    image: localhost/superior/ichor:0.0.1
    pull_policy: never
    container_name: init-migrate-seed
    restart: unless-stopped
    entrypoint: ['./admin', 'migrate-seed']
    environment:
      - ICHOR_DB_USER=postgres
      - ICHOR_DB_PASSWORD=postgres
      - ICHOR_DB_HOST=database
      - ICHOR_DB_DISABLE_TLS=true
    networks:
      ichor-system-network:
        ipv4_address: 10.5.0.10
    deploy:
      restart_policy:
        condition: none
    depends_on:
      - database

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      ichor-system-network:
        ipv4_address: 10.5.0.6

  auth:
    image: localhost/superior/auth:0.0.1
    pull_policy: never
    container_name: auth
    restart: unless-stopped
    ports:
      - "6000:6000"
      - "6010:6010"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:6000/v1/liveness || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 2
      start_period: 2s
    cpu_count: 2
    environment:
      - GOMAXPROCS=2
      - AUTH_DB_USER=postgres
      - AUTH_DB_PASSWORD=postgres
      - AUTH_DB_HOST=database
      - AUTH_DB_DISABLE_TLS=true
      - KUBERNETES_NAMESPACE=compose
      - KUBERNETES_NAME=ichor-system
      - KUBERNETES_POD_IP=10.5.0.5
      - KUBERNETES_NODE_NAME=auth
    expose:
      - "6000:6000"
      - "6010:6010"
    networks:
      ichor-system-network:
        ipv4_address: 10.5.0.5
    depends_on:
      - database

  ichor:
    image: localhost/superior/ichor:0.0.1
    pull_policy: never
    container_name: ichor
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "8090:8090"
    environment:
      - GOMAXPROCS
      - GOGC=off
      - GOMEMLIMIT
      - ICHOR_DB_USER=postgres
      - ICHOR_DB_PASSWORD=postgres
      - ICHOR_DB_HOST=database
      - ICHOR_DB_DISABLE_TLS=true
      - ICHOR_AUTH_HOST=http://auth:6000
      - ICHOR_RABBIT_MQ_URL=amqp://guest:guest@rabbitmq:5672/
      - KUBERNETES_NAMESPACE
      - KUBERNETES_NAME
      - KUBERNETES_POD_IP
      - KUBERNETES_NODE_NAME
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/v1/liveness || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 2
      start_period: 2s
    networks:
      ichor-system-network:
        ipv4_address: 10.5.0.15
    depends_on:
      - init-migrate-seed
      - rabbitmq

  metrics:
    image: localhost/superior/metrics:0.0.1
    pull_policy: never
    container_name: metrics
    restart: unless-stopped
    ports:
      - "4000:4000"
      - "4010:4010"
      - "4020:4020"
    cpus: 1
    environment:
      - GOMAXPROCS=1
      - METRICS_COLLECT_FROM=http://ichor:8090/debug/vars
    networks:
      ichor-system-network:
        ipv4_address: 10.5.0.20
    depends_on:
      - ichor

volumes:
  database: {}

networks:
  ichor-system-network:
    driver: bridge
    ipam:
      config:
        - subnet: 10.5.0.0/24
